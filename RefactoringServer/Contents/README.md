# Contents

- Content runtime modules are grouped by content category.
- Example categories:
  - `Echo`
  - `Chat`
  - `Login`
- Packet schema should follow the same category grouping under `Packet/`.
