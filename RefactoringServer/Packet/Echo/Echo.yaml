content: Echo

messages:
  - name: Echo
    rq:
      opcode: 1000
      fields:
        - { name: message, type: string }
    rp:
      opcode: 1001
      fields:
        - { name: message, type: string }
    noti:
      opcode: 1002
      fields:
        - { name: message, type: string }
