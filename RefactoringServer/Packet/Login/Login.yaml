content: Login

messages:
  - name: Login
    rq:
      opcode: 2000
      fields:
        - { name: userId, type: uint32 }
    rp:
      opcode: 2001
      fields:
        - { name: userId, type: uint32 }
        - { name: success, type: bool }
